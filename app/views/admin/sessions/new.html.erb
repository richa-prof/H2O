<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="row">
    <div class="col-12 mt-1 text-center">
      <h3>Agência H2O | Interno</h3>
    </div>
    <div class="col-12 mt-1 text-center">
      <%= f.email_field :email, autofocus: true, placeholder: "Email", required: :required, class: "form-control" %>
    </div>
    <div class="col-12 mt-1 text-center">
      <%= f.password_field :password, autocomplete: "off", placeholder: "Password", required: :required, class: "form-control" %>
    </div>
    <div class="col-12 mt-1 text-center">
      <label>
        <%= f.check_box :remember_me %> Lembrar de mim neste computador
      </label>
    </div>
    <div class="col-12 mt-1 text-center">
      <button type="submit" class="btn btn-primary">
        Entrar
      </button>
    </div>
  </div>
<% end %>
